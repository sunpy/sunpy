The :meth:`sunpy.map.GenericMap.contour` method, which still uses skimage, will now raise a deprecation warning. Please use :meth:`sunpy.map.GenericMap.get_contours`, which defaults to using ContourPy, instead.

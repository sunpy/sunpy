Added the ability to dynamically scale `sunpy.visualization.animator` instances.
By specifying a ``clip_interval``, it will now clip the minimum and maximum at each slider step to the specified interval.

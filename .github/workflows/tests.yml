name: Automated tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  core-tests:
    uses: dstansby/sunpy/.github/workflows/python-test-workflow.yml@gh-actions
    with:
      python-version: '3.9'
      tox-env: py39
